server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dostavka24
    username: postgres
    password: Yaironman@.26
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  springdoc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger-ui.html
      enabled: true

